// Earn Leverage Deleverage Borrow Repay
// [Choose Asset] [Conditionally render input component]
//

import Card from './common/Card'

// handle top input component conditionally
// - FirstInput = for all leverage, deleverage, borrow, repay
// - SecondInput = for all
// handle

// when Tabs switch, clear all inputs

const UserInputTabs = () => {
  return (
    <Card className="bg-white">
      <div>hi</div>
    </Card>
  )
}

export default UserInputTabs
